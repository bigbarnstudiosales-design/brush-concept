{
  "name": "brush-concept",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "tsc -p tsconfig.test.json && node dist-tests/testRunner.js"
  },
  "devDependencies": {
    "@types/node": "^24.12.2",
    "typescript": "^5.9.2",
    "vite": "^7.1.4"
  }
}
